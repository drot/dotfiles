#!/usr/bin/env bash

# Pick action
case $1 in
    open)
        prompt="Open URL:"
        ;;
    copy)
        prompt="Copy URL:"
        ;;
    *)
        echo "Invalid argument."
        exit 1
        ;;
esac

# Prompt based on action
rofi -dmenu -i -l 10 -w "$WINDOWID" -p "$prompt"
