;;; raindel-conf.el --- RainbowDelimiters configuration

;; Enable RainbowDelimiters for programming modes
(add-hook 'prog-mode-hook 'rainbow-delimiters-mode)

(provide 'raindel-conf)

;;; raindel-conf.el ends here
