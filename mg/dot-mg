# Save backup files to ~/.mg.d
backup-to-home-directory

# Don't touch backups in /tmp
leave-tmpdir-backups

# Display column number in the modeline
column-number-mode

# Auto indentation
auto-indent-mode

# Use C mode where appropriate
auto-execute *.c c-mode
